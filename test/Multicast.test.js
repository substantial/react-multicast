import React from 'react'
import {render} from 'react-dom'
import {MulticastProvider} from '../src'

describe('Mulitcast', () => {
  it('', () => {
    render(<MulticastProvider></MulticastProvider>, document.createElement('x-root'))
  })
})
